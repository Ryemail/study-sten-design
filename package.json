{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.8"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --scope=@sten-design/components",
    "generate": "cd ./packages/sten-components && pnpm generate"
  }
}